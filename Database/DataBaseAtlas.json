// ======================
//      Tabla User  
// ====================

// Crear colecion llamada 'User'

// Validaciones tabla User
{
    $jsonSchema: {
      bsonType: 'object',
      title: 'Validacion Usuario',
      required: [
        'userName',
        'email',
        'password',
        'type'
      ],
      properties: {
        userName: {
          bsonType: 'string',
          description: 'tipo string, requerido'
        },
        email: {
          bsonType: 'string',
          description: 'tipo string, requerido'
        },
        password: {
          bsonType: [
            'string'
          ],
          description: 'tipo string, requerido'
        },
        type: {
          'enum': [
            'client',
            'admin',
            'company'
          ],
          description: '\'type\' debe ser client, admin o company y es obligatorio'
        },
        state: {
          bsonType: [
            'int'
          ],
          description: 'debe ser un tipo entero, cuando exista'
        }
      }
    }
}